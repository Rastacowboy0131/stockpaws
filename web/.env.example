# ==== STOCKPAWS server env (Vercel -> Settings -> Environment Variables) ====
# Postgres connection string (e.g. Railway public URL). Server-only secret.
DATABASE_URL=postgresql://user:password@host:port/dbname
